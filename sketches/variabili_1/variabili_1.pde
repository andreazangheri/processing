//tipi di variabili

/*variabile boolean
  può avere due stati
  acceso || spento
  true || false */
  
boolean zeroUno = true;     // var nome = valore;

char carattere = 'g';         //variabile carattere

int numeroIntero = 1;       //variabile numero intero

float numeroVirgola = 0.1;  //variabile numero con virgola